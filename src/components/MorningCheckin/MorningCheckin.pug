.checkin-container
  checkin-header#morning-header(
    data-prop:title="Morning Intentions"
    data-prop:description="Set your intentions and focus for the day ahead"
  )

  .checkin-body
    reflection-tips(data-prop:title="Morning Reflection Tips")
      li Choose one specific practice to focus on rather than trying to do everything
      li Connect your practice to situations you'll likely encounter today
      li Think about how your core value shows up in your daily interactions
      li Set realistic, achievable intentions that you can actually practice

    .checkin-main
      card-component
        form.checkin-form(data-action="submit:handleSubmit" novalidate)
          message-component#error-message(
            data-prop:type="error"
            data-prop:role="alert"
            data-prop:aria-live="assertive"
          )
          message-component#success-message(
            data-prop:type="success"
            data-prop:role="status"
            data-prop:aria-live="polite"
          )

          .form-group
            label(for="practice-intention")
              span.label-text Today I want to practice...
              span.label-hint What skill, mindset, or behavior do you want to focus on today?
            textarea(
              id="practice-intention"
              name="practice_intention"
              rows="3"
              placeholder="e.g., active listening, patience, curiosity..."
              data-action="input:updatePracticeIntention"
              required
              minlength="3"
              aria-required="true"
            )

          .form-group
            label(for="core-value")
              span.label-text Core value I will focus on...
              span.label-hint Which of your core values will guide your decisions today?
            textarea(
              id="core-value"
              name="core_value_focus"
              rows="3"
              placeholder="e.g., compassion, integrity, growth, connection..."
              data-action="input:updateCoreValueFocus"
              required
              minlength="3"
              aria-required="true"
            )

          .form-actions
            button(type="submit" class="btn btn-primary")
              | {{submitButtonText}}
            button(type="button" class="btn btn-secondary" data-action="click:cancel")
              | Cancel