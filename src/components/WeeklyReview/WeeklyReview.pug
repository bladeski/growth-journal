.review-container
  checkin-header.review-header#weekly-header(
    data-prop:title="Weekly Review"
    data-prop:description="Reflect on your growth and patterns from this week"
  )

  .checkin-body
    reflection-tips(data-prop:title="Weekly Review Tips")
      ul
        li Be honest about both successes and setbacks - both are valuable learning
        li Look for patterns in what triggers defensive reactions
        li Celebrate small improvements - growth happens gradually
        li Use setbacks as data, not evidence of failure
        li Consider asking trusted friends for honest feedback

    .checkin-main
      card-component
        form.review-form(data-action="submit:handleSubmit" novalidate)
          message-component#error-message(
            data-prop:type="error"
            data-prop:role="alert"
            data-prop:aria-live="assertive"
          )
          message-component#success-message(
            data-prop:type="success"
            data-prop:role="status"
            data-prop:aria-live="polite"
          )

          .form-group.required
            label(for="skill-focused")
              span.label-text Skill I focused on most
              span.label-hint What specific skill, habit, or quality did you practice most this week?
            input(
              type="text"
              id="skill-focused"
              name="skill_focused_on"
              placeholder="e.g., active listening, patience, curiosity..."
              required
              minlength="3"
              aria-required="true"
              data-action="input:updateSkillFocusedOn"
            )

          .form-group
            label(for="situation-better")
              span.label-text One situation where I handled things better than before
              span.label-hint Describe a moment where you applied your growth and saw improvement
            textarea(
              id="situation-better"
              name="situation_handled_better"
              rows="3"
              placeholder="e.g., When my colleague disagreed with me, I asked questions instead of defending..."
              data-action="input:updateSituationHandledBetter"
            )

          .form-group
            label(for="situation-old-patterns")
              span.label-text One situation where I slipped back into old patterns
              span.label-hint Notice without judgment - what old habits showed up this week?
            textarea(
              id="situation-old-patterns"
              name="situation_old_patterns"
              rows="3"
              placeholder="e.g., I got defensive when someone questioned my decision and shut down..."
              data-action="input:updateSituationOldPatterns"
            )

          .form-group
            label(for="what-triggered")
              span.label-text What triggered me?
              span.label-hint What situations, words, or circumstances tend to activate your defensiveness?
            textarea(
              id="what-triggered"
              name="what_triggered_me"
              rows="2"
              placeholder="e.g., Being questioned in front of others, feeling misunderstood..."
              data-action="input:updateWhatTriggeredMe"
            )

          .form-group
            label(for="try-differently")
              span.label-text What I'll try differently next week
              span.label-hint Based on this week's experiences, what will you experiment with?
            textarea(
              id="try-differently"
              name="try_differently_next_week"
              rows="3"
              placeholder="e.g., Take a breath before responding, ask clarifying questions..."
              data-action="input:updateTryDifferentlyNextWeek"
            )

          .form-group
            label(for="partner-feedback")
              span.label-text Feedback I received from accountability partner (if any)
              span.label-hint What insights did others share about your growth this week?
            textarea(
              id="partner-feedback"
              name="accountability_partner_feedback"
              rows="3"
              placeholder="e.g., They noticed I was more patient during our discussion..."
              data-action="input:updateAccountabilityPartnerFeedback"
            )

          .form-group
            label(for="empathy-rating")
              span.label-text Self-rating (1–10) on empathy this week
              span.label-hint How well did you understand and connect with others' experiences?
            .rating-container
              input(
                type="range"
                id="empathy-rating"
                name="empathy_self_rating"
                min="1"
                max="10"
                class="rating-slider"
                data-action="input:updateEmpathySelfRating"
              )
              .rating-display
                span.rating-value
                | /10
            .rating-labels
              span 1 - Struggled
              span 5 - Average
              span 10 - Excellent

          .form-actions
            button(type="submit" class="btn btn-primary")
              | {{submitButtonText}}
            a.btn.btn-secondary(data-href="#/")
              | Back to Dashboard